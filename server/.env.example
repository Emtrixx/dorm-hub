# Copy to server/.env and fill in. Loaded by app.js via dotenv (non-production only);
# in production/Docker, set these as real environment variables.
JWT_SECRET=
PORT=8081
DB_URL=mongodb://localhost:27017/dorm-hub
